# 1월 29일

```bash
p.162 기본 매개변수
 - 함수를 호출할 때 매개변수를 지정하지 않으면 함수가 제대로 작동되지 않으니, 함수를 정의 할때 디폴트 값을 뒤에서 부터 정의( 함수가 호출 될땐 앞 매개변수부터 정의되니 뒤에부터해서 누락 되지 않게)
 
fucntion test(..,values) # 연속된 값(배열과같은) 객체가 들어온다. 매개변수로  이것을 전개 연산자
... 배열을 전개 하라.   #...이 없으면 배열이 통째로 변수로 들어가버림, 전개되는것이 아니라
```

### 객체

```bash
객체를 만들때 공백을 넣기를 워하면 ''로 묶어준다
console.log(person);
console.log(person.name);
console.log(person['name']);   # 속성값을 불러올떄는 이렇게, 공백이 포함된 속성은 대괄호로 묶어서 이렇게 표시 해줘야 뽑힘

객체에 정의되어 있는 함수를 메소드 라고함

객체내의 변수를 메소드에서 이용할 경우. this. 을 이용해서 그 객체내 변수를 사용한다는 길을 지정 해줘야함.

 console.log(`score 객체에 java정수 항목이 포함되어 있나용?${'java' in score}`);
 # 객체에 속성이 포함되어 있나 없나 확인
 with(객체이름){}  #동일한 객체에서 반복 명령을 할때 포괄해서 사용할 수 있게 해줌
 
 p,185 객체와 배열을 이용한 데이터 관리
 객체 (속성값,속성값)을 배열에 차곡차곡 쌓아서 데이터 관리
 --------> 객체
 |
 |
 V 배열
 
```

```bash
레퍼런스 타입: 값(데이터)의 주소를 가지고 있는애
밸루 타입: 값(데이터)를 가지고 있는 애

배열은 레퍼런스(주소를 복사하기) 때문에 할당 받은 변수에서 값을 바꿔도 그 주소를 가진 애의 값이 바뀌므로 할당해준 애도 값이 바뀜

깊은복사 ( 밸류 값을 바꿈, 주소값만 바꾸는게 아니라)

전개 연산자나, clone 함수를 만들어서 복사 할 수 있음

바벨 : 버전이 다른 코드 들을 서로 호환되게 해주는...??
```

## j query

* CDN : 콘텐츠를 효율적으로 전달하기 위해 전 세계 어려지점의 서버에 파일을 저장해두고, 사용자와 가까운 지역에서 해당 파일을 제공해주는 네트워크 시스템을 의미합니다.

```bash
$ = jquery
태크의 위치를 찾는 애, selector 선택자

<p> 는 뒤에 공백이 있더라도 다음줄, <span>은 뒤에 공백이 있으면 그 뒤로 작성. 블록 요소

텍스트 데코레이션은: 상위에 명령어 가하면 하위에 있는 애들도 상속 받아 적용됨
보더 어쩌구 : 따로따로
```

