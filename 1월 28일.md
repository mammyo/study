# 1월 28일

```bash
PS C:\javascript> npx http-serve #npx = node package module
<div id="display"></div> # id는 유일하게 식별 할 수 있는 키워드?? div:엘리먼트, 요소, id=속성, 디스플레이는 속성값
 window.onload = function(){}   # 헤드보다 밑 바디를 먼저 실행해야 할경우 이 명령어 사용. 스크립트 한번 싹 실행하고 이거 실행해라
```

```bash
1) div
- division의 약자
- 레이어를 만들때 가장 많이 사용되는 태그
- 블록 레벨 요소
- 줄 바꿈이 일어나 상하로 분리 됨
- 한줄 전체를 영역으로 함 (가로의길이가 100%를 차지함.)
- 웹사이트 화면 전체의 레이아웃을 잡을때 효과적으로 사욤됨
=> Float속성을 사용하여 div태그로 전체 레이아웃을 조정할수있음.
- 
2) span
- div와 비슷하게 사용되나 인라인 엘리먼트임
- 한줄로 표현되는 인라인 요소
- 줄 바꿈을 하지 않으며 한 줄로 표시
- 안에 내용 만큼만 영역으로 차지

*<span>태그 : "문장 단위로 텍스트 영역을 지정하는것" 
<span>단어</span>단어   

*<div>태그 : "문단 단위로 영역을 지정"
<div>단어</div>단어

<pre> 태그는 Preformatted Text 의 약자입니다. <pre> 태그로 감싼 문장은 입력한 문장 형태 그대로 브라우저에 표현할 수 있습니다. 예를 들어 엔터(Enter), 탭(Tab), 스페이스바(Space) 는 pre 를 사용하지 않았을 때 무조건 공백 하나로 인식합니다. 하지만 pre 를 사용하게 되면 HTML 형태 그대로 유지가 가능합니다. 그래서 프로그램 소스를 표현하는데 많이 사용합니다. # 문자 들어간 그대로 해석

innerText 와 innerHTML은 출력해주는 형식이 다르기 때문에 \t,\n 등 명령어가 다르게 찍힘
<br> html 과 만나면 다음줄로 내려가짐
```

```bash
입력 => 매개변수 = 파라미터 =인자(값)
   |
   V
함수(규칙)
   |
   V
출력 => 변환값 = 리턴값

이름이 없는 함수 = 익명함수, 있는 함수 = 기명함수
자바스크립트는 함수를 변수처럼 주고 받을 수 있다.

```

```bash
함수 바로 지정하는거랑, 호출문이 먼저 나와도 상관없음( 최신 정의한 함수로 들고옴)
변수에 지정하는것( 호출문이 먼저 나오면 언디파인드가 나옴. 관리 용이하게 하기위해 변수 지정하는게 좋음
예스 24 --->>> 인사이드 자바 스크립트
```

```bash
모든 함수는 arguments를 쓸 수있다
```

```bash
함수 내부에서 함수를 정의 # 밖에서 같은이름의 함수와 겹치지 않게  내부함수를 이용하는 외부함수 안에 넣어 사용한다.

자기 호출 함수 # 생성하자마자 한번 호출되는 함수

콜백함수 # 함수가 끝날때 호출해 달라는 개념 ( 끝났다!! 메시지가 발생)
이벤트 핸들러  #함수를 매개 변수로 넣을 수 있다는 개념
```

```bash
함수를 리턴하는 함수 인 경우 리턴값이 함수이므로 ()() 괄호를 두개 써줘야한다.

리턴되는 함수를 클로저 라고 부르기도 함. 살아있는 아웃풋 함수 # 반환하는 값이 함수여야함. 함수내부에서 사용된 변수를 함수 호출 이후에 사용할 수 있다. 지연시킨다.
```





